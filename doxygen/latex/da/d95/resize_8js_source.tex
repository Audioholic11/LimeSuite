\subsection{resize.\+js}
\label{resize_8js_source}\index{/home/erik/prefix/default/src/gr-\/radar-\/dev/build/docs/doxygen/html/resize.\+js@{/home/erik/prefix/default/src/gr-\/radar-\/dev/build/docs/doxygen/html/resize.\+js}}

\begin{DoxyCode}
00001 var cookie_namespace = \textcolor{stringliteral}{'doxygen'}; 
00002 var sidenav,navtree,content,header;
00003 
00004 \textcolor{keyword}{function} readCookie(cookie) 
00005 \{
00006   var myCookie = cookie_namespace+\textcolor{stringliteral}{"\_"}+cookie+\textcolor{stringliteral}{"="};
00007   \textcolor{keywordflow}{if} (document.cookie) 
00008   \{
00009     var index = document.cookie.indexOf(myCookie);
00010     \textcolor{keywordflow}{if} (index != -1) 
00011     \{
00012       var valStart = index + myCookie.length;
00013       var valEnd = document.cookie.indexOf(\textcolor{stringliteral}{";"}, valStart);
00014       \textcolor{keywordflow}{if} (valEnd == -1) 
00015       \{
00016         valEnd = document.cookie.length;
00017       \}
00018       var val = document.cookie.substring(valStart, valEnd);
00019       \textcolor{keywordflow}{return} val;
00020     \}
00021   \}
00022   \textcolor{keywordflow}{return} 0;
00023 \}
00024 
00025 \textcolor{keyword}{function} writeCookie(cookie, val, expiration) 
00026 \{
00027   \textcolor{keywordflow}{if} (val==undefined) \textcolor{keywordflow}{return};
00028   \textcolor{keywordflow}{if} (expiration == null) 
00029   \{
00030     var date = \textcolor{keyword}{new} Date();
00031     date.setTime(date.getTime()+(10*365*24*60*60*1000)); \textcolor{comment}{// default expiration is one week}
00032     expiration = date.toGMTString();
00033   \}
00034   document.cookie = cookie_namespace + \textcolor{stringliteral}{"\_"} + cookie + \textcolor{stringliteral}{"="} + val + \textcolor{stringliteral}{"; expires="} + expiration+\textcolor{stringliteral}{"; path=/"};
00035 \}
00036  
00037 \textcolor{keyword}{function} resizeWidth() 
00038 \{
00039   var windowWidth = $(window).width() + \textcolor{stringliteral}{"px"};
00040   var sidenavWidth = $(sidenav).outerWidth();
00041   content.css(\{marginLeft:parseInt(sidenavWidth)+\textcolor{stringliteral}{"px"}\}); 
00042   writeCookie(\textcolor{stringliteral}{'width'},sidenavWidth, null);
00043 \}
00044 
00045 \textcolor{keyword}{function} restoreWidth(navWidth)
00046 \{
00047   var windowWidth = $(window).width() + \textcolor{stringliteral}{"px"};
00048   content.css(\{marginLeft:parseInt(navWidth)+6+\textcolor{stringliteral}{"px"}\});
00049   sidenav.css(\{width:navWidth + \textcolor{stringliteral}{"px"}\});
00050 \}
00051 
00052 \textcolor{keyword}{function} resizeHeight() 
00053 \{
00054   var headerHeight = header.outerHeight();
00055   var footerHeight = footer.outerHeight();
00056   var windowHeight = $(window).height() - headerHeight - footerHeight;
00057   content.css(\{height:windowHeight + \textcolor{stringliteral}{"px"}\});
00058   navtree.css(\{height:windowHeight + \textcolor{stringliteral}{"px"}\});
00059   sidenav.css(\{height:windowHeight + \textcolor{stringliteral}{"px"}\});
00060 \}
00061 
00062 \textcolor{keyword}{function} initResizable()
00063 \{
00064   header  = $(\textcolor{stringliteral}{"#top"});
00065   sidenav = $(\textcolor{stringliteral}{"#side-nav"});
00066   content = $(\textcolor{stringliteral}{"#doc-content"});
00067   navtree = $(\textcolor{stringliteral}{"#nav-tree"});
00068   footer  = $(\textcolor{stringliteral}{"#nav-path"});
00069   $(\textcolor{stringliteral}{".side-nav-resizable"}).resizable(\{resize: \textcolor{keyword}{function}(e, ui) \{ resizeWidth(); \} \});
00070   $(window).resize(\textcolor{keyword}{function}() \{ resizeHeight(); \});
00071   var width = readCookie(\textcolor{stringliteral}{'width'});
00072   \textcolor{keywordflow}{if} (width) \{ restoreWidth(width); \} \textcolor{keywordflow}{else} \{ resizeWidth(); \}
00073   resizeHeight();
00074   var url = location.href;
00075   var i=url.indexOf(\textcolor{stringliteral}{"#"});
00076   \textcolor{keywordflow}{if} (i>=0) window.location.hash=url.substr(i);
00077   var \_preventDefault = \textcolor{keyword}{function}(evt) \{ evt.preventDefault(); \};
00078   $(\textcolor{stringliteral}{"#splitbar"}).bind(\textcolor{stringliteral}{"dragstart"}, \_preventDefault).bind(\textcolor{stringliteral}{"selectstart"}, \_preventDefault);
00079   $(document).bind(\textcolor{stringliteral}{'touchmove'},\textcolor{keyword}{function}(e)\{
00080     var device = navigator.userAgent.toLowerCase();
00081     var ios = device.match(/(iphone|ipod|ipad)/);
00082     \textcolor{keywordflow}{if} (ios) \{
00083       \textcolor{keywordflow}{try} \{
00084         var target = e.target;
00085         \textcolor{keywordflow}{while} (target) \{
00086           \textcolor{keywordflow}{if} ($(target).css(\textcolor{stringliteral}{'-webkit-overflow-scrolling'})==\textcolor{stringliteral}{'touch'}) \textcolor{keywordflow}{return};
00087           target = target.parentNode;
00088         \}
00089         e.preventDefault();
00090       \} \textcolor{keywordflow}{catch}(err) \{
00091         e.preventDefault();
00092       \}
00093     \}
00094   \});
00095 \}
00096 
00097 
\end{DoxyCode}
